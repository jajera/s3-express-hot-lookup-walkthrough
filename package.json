{
  "name": "s3-express-hot-lookup-walkthrough",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Guided CLI walkthrough for S3 Express One Zone shared hot lookup",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/check-placeholders.mjs && node scripts/check-asides.mjs && node scripts/check-references.mjs",
    "generate:og-image": "tsx scripts/generate-og-image.ts",
    "build": "npm run generate:og-image && npm run validate && astro build",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.3.1",
    "@astrojs/starlight": "^0.42.1",
    "astro": "^7.3.3",
    "astro-mermaid": "^2.1.0",
    "mermaid": "^11.17.2",
    "sharp": "^0.35.4",
    "starlight-base-path": "^0.2.1",
    "starlight-image-zoom": "^0.16.0"
  },
  "devDependencies": {
    "tsx": "^4.23.15",
    "typescript": "~5.9.3",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "js-yaml": "4.3.2",
    "markdown-it": "^14.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
